/**
 * Shared cell styles for every backoffice table so the lists read as one
 * system: a quiet uppercase header row, comfortable row height and
 * right-aligned figures in tabular numerals.
 */
export const thClass =
    'px-5 py-2.5 text-left text-[11px] font-semibold tracking-wide text-muted-foreground uppercase whitespace-nowrap';
export const tdClass = 'px-5 py-3 align-middle';
export const numericClass = 'text-right tabular-nums whitespace-nowrap';
export const rowClass = 'transition-colors hover:bg-muted/40';
export const theadClass = 'border-b border-border/80 bg-canvas/60';
